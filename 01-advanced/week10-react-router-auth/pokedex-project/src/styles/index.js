// archivo barril para poder exportar todo desde un solo lugar
// asi te permite importar desde un solo archivo (en main.tsx por ejemplo)

// import {colors} from "./colors";
import {reset, global} from "./global";
// import {typography} from "./typography";

// export {colors, reset, global, typography};
export { reset, global };